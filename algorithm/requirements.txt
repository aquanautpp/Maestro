# Core audio processing
numpy>=1.24.0
scipy>=1.10.0

# Audio I/O
soundfile>=0.12.0
sounddevice>=0.4.6  # Real-time microphone capture
pydub>=0.25.0  # For MP3 support

# Voice Activity Detection
webrtcvad>=2.0.10

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Development
black>=23.0.0
mypy>=1.0.0
ruff>=0.1.0

# Configuration
pyyaml>=6.0.0

# Web (real-time dashboard)
flask>=3.0.0
flask-socketio>=5.3.0
gunicorn>=21.0.0  # Production server for Railway

# LLM coaching
anthropic>=0.18.0  # Claude API (primary)
openai>=1.0.0  # OpenAI API (optional fallback)

# Knowledge base / RAG
sentence-transformers>=2.2.0  # Local embeddings for semantic search
PyPDF2>=3.0.0  # PDF parsing for research papers

# Web
flask-cors>=4.0.0  # CORS support for API
